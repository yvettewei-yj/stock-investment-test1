# 推送到 GitHub 并部署到 Vercel

## ✅ 已完成
- ✅ Git 仓库已初始化
- ✅ 代码已提交
- ✅ 远程仓库已配置

## 📤 推送到 GitHub

### 方法 1: 使用 Personal Access Token（推荐）

1. **获取 GitHub Personal Access Token**：
   - 访问：https://github.com/settings/tokens
   - 点击 "Generate new token" → "Generate new token (classic)"
   - 填写名称（如：`stock-investment-test`）
   - 选择过期时间（建议选择 90 天或自定义）
   - 勾选 `repo` 权限
   - 点击 "Generate token"
   - **重要**：复制生成的 token（只显示一次）

2. **推送代码**：
   ```bash
   cd ~/Desktop/static
   ./推送到GitHub.sh
   ```
   
   当提示输入用户名时：
   - Username: `yvettewei-yj`
   - Password: **粘贴你的 Personal Access Token**（不是密码）

### 方法 2: 使用 SSH（如果已配置）

如果你已经配置了 SSH 密钥，可以改用 SSH URL：

```bash
cd ~/Desktop/static
git remote set-url origin git@github.com:yvettewei-yj/stock-investment-test1.git
git push -u origin main
```

### 方法 3: 使用 GitHub CLI

如果你安装了 GitHub CLI：

```bash
cd ~/Desktop/static
gh auth login
git push -u origin main
```

## 🚀 部署到 Vercel

推送成功后：

1. **访问 Vercel**：
   - 打开 https://vercel.com
   - 登录你的账号（如果没有，可以用 GitHub 账号登录）

2. **导入项目**：
   - 点击 "Add New..." → "Project"
   - 在 "Import Git Repository" 中找到 `yvettewei-yj/stock-investment-test1`
   - 点击 "Import"

3. **配置项目**：
   - Framework Preset: 选择 "Other" 或 "Vite"（Vercel 会自动检测）
   - Root Directory: `./`（默认）
   - Build Command: 留空（静态站点）
   - Output Directory: `./`（默认）

4. **部署**：
   - 点击 "Deploy"
   - 等待部署完成（通常 1-2 分钟）

5. **访问网站**：
   - 部署完成后，Vercel 会提供一个 URL（如：`stock-investment-test1.vercel.app`）
   - 或者使用你之前配置的域名

## ✅ 验证部署

部署成功后，访问你的网站，应该：
- ✅ 不再出现 404 错误
- ✅ `/api/stocks` 返回股票数据
- ✅ `/api/streak/status` 返回连胜状态
- ✅ `/api/mascot/status` 返回吉祥物状态
- ✅ 可以正常生成学习地图

## 🔧 如果遇到问题

### 推送失败
- 确保 Personal Access Token 有 `repo` 权限
- 检查网络连接
- 尝试使用 SSH 方式

### Vercel 部署失败
- 检查 `vercel.json` 配置是否正确
- 确保 `package.json` 存在
- 查看 Vercel 部署日志中的错误信息

### API 仍然 404
- 检查 Vercel 函数日志
- 确保 API 文件在 `api/` 目录下
- 验证文件格式（CommonJS）

## 📝 快速命令参考

```bash
# 推送到 GitHub
cd ~/Desktop/static
./推送到GitHub.sh

# 或者手动推送
git push -u origin main
```

